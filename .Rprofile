   if(interactive()){
       library(colorout)
       library(setwidth)
       library(vimcom.plus)
       # If you can't install the vimcom.plus package, do:
       # library(vimcom)
   }
   options(repos = c(CRAN = "http://cran.rstudio.com"))
